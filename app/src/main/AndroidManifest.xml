<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/ahven"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ahven"
        android:supportsRtl="true"
        android:theme="@style/Theme.KalaKartta">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.TripNotesActivity" />
        <activity 
            android:name=".ui.EditCatchActivity"
            android:windowSoftInputMode="adjustPan" />
        <activity 
            android:name=".ui.FilterActivity"
            android:windowSoftInputMode="adjustPan" />
        <activity android:name=".ui.WeatherUpdateActivity" />
        <activity android:name=".ui.WeatherErrorLogActivity" />
        <activity android:name=".ui.SummaryActivity" />
        <activity android:name=".ui.EditSpeciesActivity" />
        <activity android:name=".ui.EditSpeciesDetailActivity" />
        <activity android:name=".ui.FishingSessionActivity" />

        <service
            android:name=".service.FishingSessionService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="location" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>

</manifest>